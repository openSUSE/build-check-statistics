{
  obs => 'https://build.opensuse.org/public',

  # projects => ['Virtualization'],
  projects => ['openSUSE:Factory'],

  # sqlite   => 'sqlite:test.db'
  sqlite => 'sqlite:/home/rpmlint/build-check-statistics/rpmlint.db'
}
